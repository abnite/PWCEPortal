@model PWCEPortal.ViewModel.Student.EducationHistoryViewModel
@{
    ViewBag.Title = "Add Education History";
    Layout = "_AdminLayout";
}

<div class="container-fluid px-4">
    <div class="card border-0 shadow-lg rounded-lg overflow-hidden">
        <div class="card-header bg-gradient-primary text-white d-flex justify-content-between align-items-center py-3">
            <div class="d-flex align-items-center">
                <i class="fas fa-graduation-cap fa-2x me-3"></i>
                <h4 class="mb-0">Add Education History</h4>
            </div>
            <a asp-action="EditProfile" asp-route-studentId="@Model.StudentId" class="btn btn-outline-light btn-sm">
                <i class="fas fa-arrow-left me-1"></i> Back
            </a>
        </div>
        
        <div class="card-body p-4">
            <form asp-action="AddEducationHistory" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                <input type="hidden" asp-for="StudentId" />

                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="SchoolName" class="form-label fw-bold">
                                <i class="fas fa-school me-2"></i>School Name
                            </label>
                            <input asp-for="SchoolName" class="form-control form-control-lg" placeholder="Enter school name" />
                            <span asp-validation-for="SchoolName" class="text-danger small"></span>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="OfficeHeld" class="form-label fw-bold">
                                <i class="fas fa-user-tie me-2"></i>Office Held
                            </label>
                            <input asp-for="OfficeHeld" class="form-control form-control-lg" placeholder="E.g. Prefect, Captain" />
                            <span asp-validation-for="OfficeHeld" class="text-danger small"></span>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="FromDate" class="form-label fw-bold">
                                <i class="fas fa-calendar-alt me-2"></i>From Date
                            </label>
                            <input asp-for="FromDate" type="date" class="form-control form-control-lg" />
                            <span asp-validation-for="FromDate" class="text-danger small"></span>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label asp-for="ToDate" class="form-label fw-bold">
                                <i class="fas fa-calendar-alt me-2"></i>To Date
                            </label>
                            <input asp-for="ToDate" type="date" class="form-control form-control-lg" />
                            <span asp-validation-for="ToDate" class="text-danger small"></span>
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-end mt-4 gap-3">
                    <a asp-action="EditProfile" asp-route-studentId="@Model.StudentId" class="btn btn-secondary px-4">
                        <i class="fas fa-times me-1"></i> Cancel
                    </a>
                    <button type="submit" class="btn btn-primary px-4">
                        <i class="fas fa-save me-1"></i> Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    body {
        background-color: #f8f9fa;
    }
    .card {
        border-radius: 0.5rem;
    }
    .card-header {
        border-radius: 0 !important;
    }
    .bg-gradient-primary {
        background: linear-gradient(135deg, #3f51b5 0%, #2196f3 100%);
    }
    .table th {
        white-space: nowrap;
        border-top: none;
    }
    .table td {
        vertical-align: middle;
    }
    .badge {
        font-weight: 500;
        padding: 0.5em 0.75em;
    }
    .table-hover tbody tr:hover {
        background-color: rgba(13, 110, 253, 0.05) !important;
    }
    .course-checkbox {
        transform: scale(1.3);
    }
    #selectAll {
        transform: scale(1.2);
    }
</style>